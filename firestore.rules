rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Single-user app: allow all reads/writes from the server SDK
      allow read, write: if true;
    }
  }
}
